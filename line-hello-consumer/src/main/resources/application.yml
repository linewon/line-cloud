server:
  port: 8201
  
spring:
  application:
    name: line-hello-consumer
eureka:
  client:
    serviceUrl:
      defaultZone: http://${peers.peer3.name}:${peers.peer3.port}/eureka/,http://${peers.peer2.name}:${peers.peer2.port}/eureka/,http://${peers.peer1.name}:${peers.peer1.port}/eureka/